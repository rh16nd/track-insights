# Where the Flask prediction API lives (api.py in the athletics-predictor repo).
#
# Optional. Left unset, the app talks to http://localhost:5000, which is the
# right default for the local development setup: `python api.py` in the
# athletics-predictor folder, `npm run dev` here.
#
# Set it when the API is anywhere else -- a deployed instance, a LAN address,
# a tunnel. Until this existed the host was hardcoded at eight call sites, so
# a build served to anyone else fetched from THEIR machine and failed on every
# page. No trailing slash needed; one is stripped if present.
#
# VITE_ prefixed vars are injected at BUILD time, not read at runtime, so a
# change here needs a rebuild.
VITE_API_BASE_URL=http://localhost:5000

# The site's own public origin, e.g. https://podiumcall.com — no trailing slash.
#
# Optional, and unset is a valid state: the structured data (JSON-LD) degrades
# rather than lying. Without it the WebSite block omits its `url` and the
# per-page BreadcrumbList is not emitted at all, because Google requires an
# absolute `item` URL on every crumb except the last and would reject a trail
# of bare names. Set this and both complete themselves — no code change.
#
# This is the same blocker as sitemap.xml and canonical tags; the Python side
# reads it as PODIUMCALL_BASE_URL (see scripts/make-sitemap.py).
# VITE_SITE_URL=https://podiumcall.com

# Web3Forms access key — where the feedback form's messages go.
#
# Optional, and unset has a defined behaviour: feedback-modal.tsx falls back to
# opening the reader's own mail client. That fallback is deliberate and stays.
# A form that posts to nowhere and says "sent" is worse than no form.
#
# NOT a secret, and it does not need Vercel's "Sensitive" flag. The key names a
# destination inbox and authorises nothing else; it is designed to sit in client
# code, and it ends up readable in the JS bundle no matter where it is stored.
#
# To set it up: create a free account at web3forms.com against the destination
# inbox, then add this as an Environment Variable in the Vercel project and
# REDEPLOY — VITE_ vars are inlined at build time, so saving the variable
# without a new build changes nothing.
# VITE_WEB3FORMS_KEY=00000000-0000-0000-0000-000000000000
